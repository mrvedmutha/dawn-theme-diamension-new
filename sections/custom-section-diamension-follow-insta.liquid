{{ 'section-diamension-follow-insta.css' | asset_url | stylesheet_tag }}

{%- liquid
  assign headline_text = section.settings.headline_text
  assign cta_text = section.settings.cta_text
  assign cta_url = section.settings.cta_url
  assign show_decorative_ellipse = section.settings.show_decorative_ellipse
-%}

<section
  class="custom-section-diamension-follow-insta"
  data-section-id="{{ section.id }}"
  data-section-type="diamension-follow-insta"
>
  <div class="custom-section-diamension-follow-insta__wrapper">
    <div class="custom-section-diamension-follow-insta__container">
      {%- comment -%} Headline {%- endcomment -%}
      {%- if headline_text != blank -%}
        <h2 class="custom-section-diamension-follow-insta__headline">
          {{ headline_text }}
        </h2>
      {%- endif -%}

      {%- comment -%} Decorative Ellipse Background (Desktop Only) {%- endcomment -%}
      {%- if show_decorative_ellipse -%}
        <div class="custom-section-diamension-follow-insta__ellipse">
          <img
            src="{{ 'custom-section-instagram/instagram-section-circle.svg' | asset_url }}"
            alt=""
            class="custom-section-diamension-follow-insta__ellipse-svg"
          >
        </div>
      {%- endif -%}

      {%- comment -%}
        Asymmetric Masonry Grid - Image Grid
        Images: 360px × 360px with 72px horizontal overlap (288px spacing)
        Vertical: 180px overlap (50% of image height)
        Total grid: 1224px width × 540px height, centered in 1440px container
      {%- endcomment -%}
      <div class="custom-section-diamension-follow-insta__images">
        {%- for block in section.blocks -%}
          {%- if block.type == 'instagram_image' and block.settings.image != blank -%}
            <div class="custom-section-diamension-follow-insta__image-wrapper custom-section-diamension-follow-insta__image-wrapper--{{ forloop.index }}" {{ block.shopify_attributes }}>
              {%- if block.settings.link_url != blank -%}
                <a
                  href="{{ block.settings.link_url }}"
                  target="_blank"
                  rel="noopener noreferrer"
                  class="custom-section-diamension-follow-insta__image-link"
                >
                  <img
                    src="{{ block.settings.image | image_url: width: 720 }}"
                    srcset="
                      {{ block.settings.image | image_url: width: 360 }} 360w,
                      {{ block.settings.image | image_url: width: 540 }} 540w,
                      {{ block.settings.image | image_url: width: 720 }} 720w
                    "
                    sizes="(max-width: 767px) 50vw, (max-width: 1024px) 50vw, 360px"
                    alt="{{ block.settings.image.alt | default: 'Instagram image' }}"
                    class="custom-section-diamension-follow-insta__image js-parallax-image"
                    loading="lazy"
                  >
                </a>
              {%- else -%}
                <img
                  src="{{ block.settings.image | image_url: width: 720 }}"
                  srcset="
                    {{ block.settings.image | image_url: width: 360 }} 360w,
                    {{ block.settings.image | image_url: width: 540 }} 540w,
                    {{ block.settings.image | image_url: width: 720 }} 720w
                  "
                  sizes="(max-width: 767px) 50vw, (max-width: 1024px) 50vw, 360px"
                  alt="{{ block.settings.image.alt | default: 'Instagram image' }}"
                  class="custom-section-diamension-follow-insta__image js-parallax-image"
                  loading="lazy"
                >
              {%- endif -%}
            </div>
          {%- endif -%}
        {%- endfor -%}
      </div>

      {%- comment -%} CTA Link {%- endcomment -%}
      {%- if cta_text != blank and cta_url != blank -%}
        <div class="custom-section-diamension-follow-insta__cta-wrapper">
          <a
            href="{{ cta_url }}"
            class="custom-section-diamension-follow-insta__cta"
            target="_blank"
            rel="noopener noreferrer"
          >
            {{ cta_text }}
          </a>
        </div>
      {%- endif -%}
    </div>
  </div>
</section>

{%- comment -%} <script src="{{ 'section-diamension-follow-insta.js' | asset_url }}" defer></script> {%- endcomment -%}
<script src="{{ 'section-diamension-follow-insta.min.js' | asset_url }}" defer></script>

{% schema %}
{
  "name": "Diamension Instagram",
  "tag": "section",
  "class": "section-diamension-follow-insta",
  "max_blocks": 4,
  "settings": [
    {
      "type": "header",
      "content": "Headline"
    },
    {
      "type": "text",
      "id": "headline_text",
      "label": "Headline Text",
      "default": "FOLLOW US ON\nINSTAGRAM",
      "info": "Use line break for two lines (FOLLOW US ON / INSTAGRAM)"
    },
    {
      "type": "header",
      "content": "Call to Action"
    },
    {
      "type": "text",
      "id": "cta_text",
      "label": "CTA Text",
      "default": "Instagram",
      "info": "Link text (e.g., 'Instagram' or 'Follow Us')"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "Instagram URL",
      "info": "Link to your Instagram profile"
    },
    {
      "type": "header",
      "content": "Design Options"
    },
    {
      "type": "checkbox",
      "id": "show_decorative_ellipse",
      "label": "Show decorative ellipse (desktop only)",
      "default": true,
      "info": "Display decorative background ellipse on desktop. Hidden on tablet/mobile."
    }
  ],
  "blocks": [
    {
      "type": "instagram_image",
      "name": "Instagram Post",
      "limit": 4,
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Instagram Image",
          "info": "Upload a square image (1:1 ratio). Recommended size: 720×720px or larger for best quality."
        },
        {
          "type": "url",
          "id": "link_url",
          "label": "Instagram Post URL",
          "info": "Link to the Instagram post. Opens in a new tab when clicked."
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Diamension Follow Instagram",
      "blocks": [
        {
          "type": "instagram_image"
        },
        {
          "type": "instagram_image"
        },
        {
          "type": "instagram_image"
        },
        {
          "type": "instagram_image"
        }
      ]
    }
  ]
}
{% endschema %}
